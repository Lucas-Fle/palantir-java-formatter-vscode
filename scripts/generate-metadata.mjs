import { writeFileSync } from "node:fs";
import path from "node:path";

import {
  formatterVersion,
  root,
  workerJarName
} from "./project-metadata.mjs";

const output = path.join(root, "extension", "src", "generatedMetadata.ts");
const contents = `// Generated by scripts/generate-metadata.mjs. Do not edit.
export const FORMATTER_VERSION = ${JSON.stringify(formatterVersion)};
export const WORKER_JAR_NAME = ${JSON.stringify(workerJarName)};
`;

writeFileSync(output, contents, "utf8");
